# End-to-end verification — 2026-09-07, musicplayer (LMS 9.1.1, macOS 12.7 Intel)

Plugin at `~/Library/Application Support/Squeezebox/Plugins/SACDPlayer`, branch `feat/sacdplayer`.
Library: 30 SACD ISOs (24 under `Lossless/SACD-ISO`, 6 elsewhere) on the SMB share.

| Step | Expected | Observed |
|---|---|---|
| Wipe + full rescan (`wipecache`) | ISOs read with `-P`, one album per area, ISO hidden | 09:43 → 10:07. 385 virtual tracks, 30 ISO rows `fec`/audio 0, 36 albums "(2ch)"/"(mch)". One ISO (Pretzel Logic) unreadable → hidden. Load stayed < 3. |
| DB rows | `dsf`, `virtual 1`, `remote 0`, secs/channels/tracknum | `...Tale Spining.iso#2ch-01 | dsf | 1 | 388.0 | 2 | 1 | 0` |
| Play *Birds Of Fire (2ch)* track 1 on Apple Squeezer | wait for extraction, then play | Play started 22 s after the request (SMB read of a 5:50 track). Tracks 2-4 extracted in the background while track 1 played; album folder 1.6 GB after 60 s. |
| Engine / DAC | DoP 176.4 kHz, bit-perfect, no underruns | CoreAudio telemetry: `mode=native rate=176400 ... underruns=0 exclusive=verified fallback=none` (DSD64 over DoP). First play 10:08-10:15: LED white but **no sound** reported. Replays at 10:29 and 10:32 (same cached file, engine kept the device open at 176.4 kHz): **audible**, confirmed by Felipe. A library DSF played in between was audible too. Offline check of the cached DSF (dsdplay → flac, 10-25 s): RMS -44.7 dBFS, peak -22.1 dBFS, i.e. real signal. The silent first play matches the Mojo's known behaviour on the first DoP open after a rate change rather than the plugin. |
| Manual prepare of *(mch)* | queued at priority 2, one worker | `sacdplayer prepare <key>/mch` → success; track 1 (612 MB, 5 ch) ready after ~7 min. |
| Play *(mch)* track 1 | plays L/R | **Fails**: engine logs `_valid_stream_format: unsupported DSD format: rate=2822400 channels=5`, LMS reports "Decoder does not support file format". The Apple Squeezer engine only accepts 2-channel DSD. → `show_mch` pref, default off; mch albums removed from the library on the next full rescan. |
| `sacdplayer evict <key>/mch` | cancels the running extraction, removes the folder | success; queue 0, folder gone, no stray `sacd_extract`. |
| `playlist play <anchored url>` | plays that track | The anchor was dropped and the container got queued ("Couldn't load module (fec)"). `playlistcontrol cmd:load track_id:N` (what Echo Classic and the web UI use) works. Noted, not fixed. |

## Incidents on the way (all fixed in the branch)
1. `InstalledPlugins/` is wiped by the extension manager on restart → deploy to the manual `Plugins/` folder.
2. `fork` inside the LMS process (the scan runs in-process on macOS; STDOUT/STDERR are tied to the log trapper) turned each child into a live server clone → 40+ LMS copies, load > 500, machine down twice. Fixed with perl's list-form pipe open.
3. `sacd://` URLs were treated as remote by LMS and never stored in the library → `file://…iso#2ch-NN` anchor URLs with a URL-regexp protocol handler, like cue sheets.
4. Player status polls on a volatile entry re-read the ISO tags every 30 s → `getTag` now returns only the container when the children already exist.
5. Unrelated but relevant on this box: `coreduetd` runaway after the crashes (reset with `launchctl kickstart -k system/com.apple.coreduetd`); the LMS menu-bar wrapper does not restart the server, and `open -a` does not either once the server has been killed.

## Measurements
- `-P` per ISO over SMB during the scan: well under 1 s each (24 ISOs in ~10 s of scan time).
- 2ch track 5:50 over SMB: 20.5 s (17× realtime). 5ch DST track 5:50: 6 min 56 s (1.2× realtime, 170 % CPU).

## Final full rescan after the `show_mch` change (10:58 → 11:51, machine freshly rebooted)
300 stereo virtual tracks, 29 "(2ch)" albums (30 ISOs, one unreadable and hidden), 0 "(mch)" albums, 30 hidden containers, **0 plugin errors** in the server log. Cached tracks from the earlier session still play immediately (the cache key is path+size+mtime, unchanged by the rescan).
